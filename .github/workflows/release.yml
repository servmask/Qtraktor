name: Release

on:
  release:
    types: [published]
  workflow_dispatch:

env:
  VERSION: ${{ github.event.release.tag_name || 'latest' }}

jobs:
  build-macos:
    strategy:
      matrix:
        include:
          - runner: macos-latest
            arch: arm64
          - runner: macos-15-intel
            arch: x86_64
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: brew install qt@5 openssl pkg-config

      - name: Build
        run: |
          export PATH="$(brew --prefix qt@5)/bin:$PATH"
          export PKG_CONFIG_PATH="$(brew --prefix openssl)/lib/pkgconfig"
          qmake Qtraktor.pro
          make -j$(sysctl -n hw.ncpu)

      - name: Bundle Qt frameworks
        run: |
          export PATH="$(brew --prefix qt@5)/bin:$PATH"
          macdeployqt Traktor.app

      - name: Archive app (preserve symlinks)
        run: tar -cf Traktor-${{ matrix.arch }}.app.tar Traktor.app

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Traktor-${{ matrix.arch }}.app
          path: Traktor-${{ matrix.arch }}.app.tar

  package-macos:
    needs: build-macos
    runs-on: macos-latest
    steps:
      - name: Download arm64 app
        uses: actions/download-artifact@v4
        with:
          name: Traktor-arm64.app
          path: arm64

      - name: Download x86_64 app
        uses: actions/download-artifact@v4
        with:
          name: Traktor-x86_64.app
          path: x86_64

      - name: Extract archives
        run: |
          tar -xf arm64/Traktor-arm64.app.tar -C arm64
          tar -xf x86_64/Traktor-x86_64.app.tar -C x86_64

      - name: Create universal binary
        run: |
          cp -R arm64/Traktor.app Universal-Traktor.app

          # Merge all Mach-O binaries with lipo
          find arm64/Traktor.app -type f | while read arm_file; do
            rel="${arm_file#arm64/Traktor.app/}"
            intel_file="x86_64/Traktor.app/$rel"
            universal_file="Universal-Traktor.app/$rel"

            # Check if both files exist and are Mach-O
            if [ -f "$intel_file" ] && file "$arm_file" | grep -q "Mach-O"; then
              echo "Merging: $rel"
              lipo -create "$arm_file" "$intel_file" -output "$universal_file"
            fi
          done

      - name: Codesign universal app
        run: |
          # Sign frameworks and dylibs inside-out before signing the app
          find Universal-Traktor.app/Contents/Frameworks -name "*.framework" -type d | while read fw; do
            codesign --force --sign - "$fw"
          done
          find Universal-Traktor.app/Contents/Frameworks -name "*.dylib" -type f | while read lib; do
            codesign --force --sign - "$lib"
          done
          find Universal-Traktor.app/Contents/PlugIns -type f -name "*.dylib" 2>/dev/null | while read lib; do
            codesign --force --sign - "$lib"
          done
          codesign --force --sign - Universal-Traktor.app

      - name: Rename app before checkout
        run: mv Universal-Traktor.app /tmp/Traktor.app

      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            icons/background.png
            icons/.VolumeIcon.icns
          sparse-checkout-cone-mode: false

      - name: Create DMG
        run: |
          brew install create-dmg fileicon
          mv /tmp/Traktor.app Traktor.app

          # Create DMG with background, icon layout, and Applications symlink
          create-dmg \
            --volname "Traktor" \
            --volicon "icons/.VolumeIcon.icns" \
            --background "icons/background.png" \
            --window-pos 200 120 \
            --window-size 576 384 \
            --icon-size 64 \
            --text-size 14 \
            --icon "Traktor.app" 125 195 \
            --app-drop-link 410 195 \
            --no-internet-enable \
            "Traktor-${{ env.VERSION }}.dmg" \
            "Traktor.app"

          # Post-process: replace symlink with Finder alias and set Applications icon
          hdiutil convert "Traktor-${{ env.VERSION }}.dmg" -format UDRW -o rw-traktor.dmg -ov
          rm "Traktor-${{ env.VERSION }}.dmg"
          hdiutil attach rw-traktor.dmg
          rm /Volumes/Traktor/Applications
          osascript -e 'tell application "Finder" to make new alias file at (POSIX file "/Volumes/Traktor" as alias) to (POSIX file "/Applications" as alias) with properties {name:"Applications"}'
          fileicon set "/Volumes/Traktor/Applications" /System/Library/CoreServices/CoreTypes.bundle/Contents/Resources/ApplicationsFolderIcon.icns
          rm -rf /Volumes/Traktor/.fseventsd
          sync && sleep 1
          hdiutil detach /Volumes/Traktor
          hdiutil convert rw-traktor.dmg -format UDZO -o "Traktor-${{ env.VERSION }}.dmg"
          rm rw-traktor.dmg

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Traktor-${{ env.VERSION }}.dmg
          path: Traktor-${{ env.VERSION }}.dmg

  build-windows:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '5.15.2'
          host: windows
          target: desktop
          arch: win64_msvc2019_64
          tools: tools_ifw
          cache: true

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Cache vcpkg packages
        uses: actions/cache@v4
        with:
          path: C:\vcpkg\installed
          # Shared key with ci.yml so both workflows benefit from the same cache
          key: vcpkg-windows-openssl-zlib
          restore-keys: |
            vcpkg-windows-

      - name: Install OpenSSL and zlib
        run: vcpkg install openssl:x64-windows zlib:x64-windows

      - name: Set vcpkg paths
        shell: pwsh
        run: |
          "OPENSSL_DIR=$env:VCPKG_INSTALLATION_ROOT\installed\x64-windows" | Out-File -FilePath $env:GITHUB_ENV -Append
          "LIB=$env:VCPKG_INSTALLATION_ROOT\installed\x64-windows\lib;$env:LIB" | Out-File -FilePath $env:GITHUB_ENV -Append
          "INCLUDE=$env:VCPKG_INSTALLATION_ROOT\installed\x64-windows\include;$env:INCLUDE" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Build
        shell: cmd
        run: |
          qmake Qtraktor.pro
          nmake -f Makefile.Release

      - name: Create installer
        shell: pwsh
        run: |
          # Bundle Qt runtime DLLs
          windeployqt release\Traktor.exe

          # Bundle vcpkg DLLs (OpenSSL + zlib)
          Copy-Item "$env:VCPKG_INSTALLATION_ROOT\installed\x64-windows\bin\libssl*.dll" -Destination release\
          Copy-Item "$env:VCPKG_INSTALLATION_ROOT\installed\x64-windows\bin\libcrypto*.dll" -Destination release\
          Copy-Item "$env:VCPKG_INSTALLATION_ROOT\installed\x64-windows\bin\zlib1.dll" -Destination release\

          # Bundle file type icon for .wpress association
          Copy-Item "icons\file.ico" -Destination release\

          # Prepare installer package data
          New-Item -ItemType Directory -Path packages\com.servmask.traktor\data -Force
          Get-ChildItem release | Copy-Item -Destination packages\com.servmask.traktor\data -Recurse

          # Replace version placeholders
          $version = "${{ env.VERSION }}"
          $date = Get-Date -Format yyyy-MM-dd
          (Get-Content "config\config.xml") -replace "develop", $version | Set-Content "config\config.xml"
          (Get-Content "packages\com.servmask.traktor\meta\package.xml") -replace "develop", $version | Set-Content "packages\com.servmask.traktor\meta\package.xml"
          (Get-Content "packages\com.servmask.traktor\meta\package.xml") -replace "release-date", $date | Set-Content "packages\com.servmask.traktor\meta\package.xml"

          # Find and run Qt IFW binarycreator
          $ifw = Get-ChildItem "$env:IQTA_TOOLS\QtInstallerFramework" -Directory | Sort-Object Name -Descending | Select-Object -First 1
          & "$($ifw.FullName)\bin\binarycreator.exe" -c config\config.xml -p packages "Traktor-$env:VERSION.exe"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Traktor-${{ env.VERSION }}.exe
          path: Traktor-${{ env.VERSION }}.exe

  build-linux:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y qtbase5-dev qt5-qmake libssl-dev zlib1g-dev pkg-config libfuse2

      - name: Build
        run: |
          qmake Qtraktor.pro
          make -j$(nproc)

      - name: Cache linuxdeploy
        uses: actions/cache@v4
        with:
          path: linuxdeploy*.AppImage
          key: linuxdeploy-continuous

      - name: Create AppImage
        run: |
          # Install linuxdeploy and Qt plugin (skipped if cached)
          [ -f linuxdeploy-x86_64.AppImage ] || wget -q https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
          [ -f linuxdeploy-plugin-qt-x86_64.AppImage ] || wget -q https://github.com/linuxdeploy/linuxdeploy-plugin-qt/releases/download/continuous/linuxdeploy-plugin-qt-x86_64.AppImage
          chmod +x linuxdeploy*.AppImage

          # Create .desktop file with file association and context menu action
          cat > traktor.desktop << 'DESKTOP'
          [Desktop Entry]
          Name=Traktor
          Comment=WPRESS Extractor
          Exec=Traktor %f
          Icon=traktor
          Type=Application
          Categories=Utility;
          MimeType=application/x-wpress;
          Actions=Extract;

          [Desktop Action Extract]
          Name=Extract with Traktor
          Exec=Traktor %f
          DESKTOP
          sed -i 's/^          //' traktor.desktop

          # Install MIME type definition
          mkdir -p AppDir/usr/share/mime/packages
          cp wpress.xml AppDir/usr/share/mime/packages/

          # Build AppImage
          ./linuxdeploy-x86_64.AppImage \
            --appdir AppDir \
            --executable Traktor \
            --desktop-file traktor.desktop \
            --icon-file icons/traktor.png \
            --plugin qt \
            --output appimage

          mv Traktor-*.AppImage Traktor-x86_64-${{ env.VERSION }}.AppImage

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Traktor-x86_64-${{ env.VERSION }}.AppImage
          path: Traktor-x86_64-${{ env.VERSION }}.AppImage

  upload:
    if: github.event_name == 'release'
    needs: [package-macos, build-windows, build-linux]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Upload assets to GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${{ github.event.release.tag_name }}" \
            Traktor-${{ env.VERSION }}.dmg/Traktor-${{ env.VERSION }}.dmg \
            Traktor-${{ env.VERSION }}.exe/Traktor-${{ env.VERSION }}.exe \
            Traktor-x86_64-${{ env.VERSION }}.AppImage/Traktor-x86_64-${{ env.VERSION }}.AppImage \
            --repo "${{ github.repository }}"

      - name: Submit to VirusTotal
        if: secrets.VT_API_KEY != ''
        env:
          VT_API_KEY: ${{ secrets.VT_API_KEY }}
        run: |
          submit_to_vt() {
            local file="$1"
            local name="$2"
            echo "Submitting $name to VirusTotal..."

            # Get upload URL (required for files that may exceed 32MB)
            upload_url=$(curl -s --request GET \
              --url "https://www.virustotal.com/api/v3/files/upload_url" \
              --header "x-apikey: $VT_API_KEY" | jq -r '.data')

            # Upload file
            response=$(curl -s --request POST \
              --url "$upload_url" \
              --header "x-apikey: $VT_API_KEY" \
              --form "file=@$file")

            analysis_id=$(echo "$response" | jq -r '.data.id // empty')
            if [ -z "$analysis_id" ]; then
              echo "::warning::Failed to submit $name to VirusTotal"
              echo "VT_${name}_LINK=" >> $GITHUB_ENV
              return
            fi

            # Wait for analysis to complete (poll up to 5 minutes)
            for i in $(seq 1 30); do
              sleep 10
              status=$(curl -s --request GET \
                --url "https://www.virustotal.com/api/v3/analyses/$analysis_id" \
                --header "x-apikey: $VT_API_KEY" | jq -r '.data.attributes.status')
              echo "  $name: $status (attempt $i/30)"
              if [ "$status" = "completed" ]; then
                break
              fi
            done

            # Get the SHA-256 from the analysis to build the permalink
            sha256=$(curl -s --request GET \
              --url "https://www.virustotal.com/api/v3/analyses/$analysis_id" \
              --header "x-apikey: $VT_API_KEY" | jq -r '.meta.file_info.sha256 // empty')

            if [ -n "$sha256" ]; then
              echo "VT_${name}=https://www.virustotal.com/gui/file/$sha256" >> $GITHUB_ENV
              echo "  $name: https://www.virustotal.com/gui/file/$sha256"
            else
              echo "VT_${name}=" >> $GITHUB_ENV
              echo "::warning::Could not get SHA-256 for $name"
            fi

            # Rate limit: free tier allows 4 req/min, wait between files
            sleep 15
          }

          submit_to_vt "Traktor-${{ env.VERSION }}.dmg/Traktor-${{ env.VERSION }}.dmg" "DMG"
          submit_to_vt "Traktor-${{ env.VERSION }}.exe/Traktor-${{ env.VERSION }}.exe" "EXE"
          submit_to_vt "Traktor-x86_64-${{ env.VERSION }}.AppImage/Traktor-x86_64-${{ env.VERSION }}.AppImage" "APPIMAGE"

      - name: Update release notes with VirusTotal links
        if: secrets.VT_API_KEY != ''
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Build VT section only with successful scans
          VT_SECTION=""
          [ -n "$VT_DMG" ] && VT_SECTION="${VT_SECTION}| macOS (.dmg) | [View scan results]($VT_DMG) |"$'\n'
          [ -n "$VT_EXE" ] && VT_SECTION="${VT_SECTION}| Windows (.exe) | [View scan results]($VT_EXE) |"$'\n'
          [ -n "$VT_APPIMAGE" ] && VT_SECTION="${VT_SECTION}| Linux (.AppImage) | [View scan results]($VT_APPIMAGE) |"$'\n'

          if [ -n "$VT_SECTION" ]; then
            EXISTING=$(gh release view "${{ github.event.release.tag_name }}" --json body -q .body --repo "${{ github.repository }}")
            APPEND=$(printf '\n\n---\n\n### VirusTotal Scan Results\n\n| File | Report |\n|------|--------|\n%s' "$VT_SECTION")
            gh release edit "${{ github.event.release.tag_name }}" \
              --notes "${EXISTING}${APPEND}" \
              --repo "${{ github.repository }}"
          fi

